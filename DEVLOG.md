# Developer Log

## v0.2.0

Today I learned comparison operators.

The biggest thing I learned was that comparison operators return Boolean values.

One thing that surprised me was that:

True == 1

returns True.

I also learned that Python can compare different data types without throwing an error.

Today I improved DeveloperOS by adding profile validation.




## v0.3.0

Today I integrated my first `if` statements into DeveloperOS.

Instead of only displaying `True` or `False`, the application now performs actions when specific conditions are met.

I also improved the overall quality of user input by cleaning text with `.strip()`, `.title()`, and `.lower()`.

One thing I noticed is that my code is beginning to repeat in some places, especially when printing section headers. I know there must be a better way to do this, and I'm looking forward to learning functions later in the course.

# Developer Log

## v0.3.0

Today I upgraded DeveloperOS by introducing `else` statements.

Previously, the application only responded when a condition was true. Now every validation provides meaningful feedback regardless of the outcome.

This version also made me think more about user experience. Instead of simply displaying "Access Denied," I learned that software should explain why an action cannot be completed.

DeveloperOS is beginning to feel like a real application rather than just a collection of Python exercises.